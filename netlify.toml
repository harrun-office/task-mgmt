[build]
  base = "client"
  publish = "dist"
  command = "npm run build"

[context.production.environment]
  # I recommend setting VITE_API_URL in Netlify UI instead of committing secrets.
  VITE_API_URL = "https://task-mgmt-xdfl.onrender.com/api"